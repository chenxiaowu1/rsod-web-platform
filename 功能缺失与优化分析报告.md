# rsod-web-platform 功能缺失与优化分析报告

## 一、当前项目概况

| 层级 | 技术栈 | 核心功能 |
|------|--------|----------|
| 前端 | Vue 3 + Element Plus + Vite + Pinia(未使用) | 6个页面：登录/注册/检测/历史/AI问答/目标库 |
| 后端 | FastAPI + YOLO11-OBB + DeepSeek | 单图/批量检测、模型热切换、SSE流式问答 |
| 基础设施 | Docker Compose (PostgreSQL + Redis + MinIO) | **均未实际接入代码，仅声明** |

---

## 二、功能缺失清单（按优先级排列）

### P0 - 致命缺失（影响平台可用性）

| 缺失功能 | 现状 | 影响 |
|----------|------|------|
| **真实认证系统** | localStorage 伪认证，无 JWT/OAuth | 无法生产使用，用户数据无安全保障 |
| **数据库接入** | PostgreSQL 已配置但代码使用 JSON 文件存储 | 历史记录无法扩展，无事务，无查询优化 |
| **对象存储接入** | MinIO 已配置但图片存本地 static/ 目录 | 无法水平扩展，大图存储受限 |

### P1 - 核心缺失（遥感检测平台必备）

| 缺失功能 | 行业标准 | 当前差距 |
|----------|----------|----------|
| **交互式地图可视化** | Leaflet/OpenLayers/Cesium，检测框叠加在底图上 | 仅 side-by-side 图片对比，无地理空间展示 |
| **手动标注工具** | 矩形框/旋转框/多边形/画笔标注 | **完全缺失**，无法修正检测结果或创建标注 |
| **标注导出** | COCO JSON / YOLO / GeoJSON / Shapefile 多格式导出 | 仅可下载标注后图片，无结构化标签导出 |
| **模型评估面板** | mAP / Precision / Recall / F1-Score / 混淆矩阵 | 仅显示检测框数量和置信度，无评估指标 |
| **置信度阈值调节** | UI 滑块调整 conf/iou threshold | 硬编码在 config.py，前端无法控制 |
| **地理影像格式支持** | GeoTIFF / COG / NTF 等遥感格式 | 仅支持 jpg/png 普通图片 |

### P2 - 重要缺失（平台完整度）

| 缺失功能 | 说明 |
|----------|------|
| **项目管理/工作区** | 无法按项目组织图片和检测结果 |
| **批量处理优化** | 当前前端顺序调用 detectSingleImage，非真正批量；无并行进度、无取消机制 |
| **结果统计仪表盘** | 缺少：每类别分布饼图、检测密度热力图、时间趋势图 |
| **PDF 报告生成** | 无自动生成检测报告功能 |
| **用户角色权限** | 无 RBAC (管理员/标注员/审核员/查看者) |
| **标注审核流程** | 无标注→审核→返工→通过的工作流 |
| **AI 辅助标注** | 无 SAM 集成、无交互式分割、无预标注功能 |
| **自定义模型上传** | 仅内置 3 个 YOLO11-OBB 模型，不支持用户上传自有模型 |
| **通知系统** | 无任务分配通知、无检测完成通知 |
| **操作日志/审计** | 无用户操作记录，无数据变更追溯 |

### P3 - 增强功能

| 缺失功能 | 说明 |
|----------|------|
| 变化检测（双时相对比） | 遥感检测平台的核心差异化功能 |
| 视频流检测 | 无人机视频实时推理 |
| 大图切片推理 | 超大遥感影像自动切片+拼接 |
| STAC 目录接入 | 标准遥感数据目录协议 |
| OGC 服务发布 | WMS/WFS/WMTS 标准接口发布结果 |
| 边缘设备部署导出 | TensorRT / ONNX / CoreML 导出 |
| 多语言国际化 | 当前仅中文 |

---

## 三、前端优化方向

### 3.1 架构层面

**问题 1：Pinia 已安装但完全未使用**

当前所有状态散落在各组件 `ref()` 中，跨组件共享通过 props 逐层传递或直接读 localStorage。需建立：

```
stores/
  auth.js       — 用户认证状态、token 管理
  detection.js  — 检测参数、当前结果、模型选择
  history.js    — 历史记录缓存、筛选条件
  ui.js         — 主题、侧边栏折叠、全局 loading
```

**问题 2：API 调用不一致**

- `DetectionPage.vue` 使用 `request.js` Axios 封装
- `QAPage.vue` 使用原生 `fetch()`
- `Inference.vue` (废弃) 使用裸 `axios`

应统一使用 `request.js`，并将 SSE 流式请求也封装进去。

**问题 3：遗留文件未清理**

| 文件 | 状态 |
|------|------|
| `src/views/Detection.vue` | 废弃，未被路由引用 |
| `src/views/Inference.vue` | 废弃，未被路由引用 |
| `src/views/test_connect.vue` | 调试文件，未被路由引用 |
| `src/components/HelloWorld.vue` | Vite 脚手架残留 |
| `src/components/SliderCompare.vue` | 有功能但未被实际使用 |

### 3.2 功能完整性

**问题 4：批量检测是伪批量**

`DetectionPage.vue` 中批量检测实际是前端循环调用 `detectSingleImage`：

```js
// 当前实现：顺序逐个请求
for (const file of files) {
  const res = await detectSingleImage(formData)
}
```

应改为调用后端真正的 `/detection/batch` 接口，并增加：
- 并发控制（最多 N 个并行）
- 取消/暂停机制
- 单文件级别的进度和失败重试

**问题 5：SliderCompare 组件未集成**

`SliderCompare.vue` 是比较原图与检测结果的最佳交互方式，但仅在被废弃的 `Detection.vue` 中使用。应集成到 `DetectionPage.vue` 的单图检测结果中。

**问题 6：缺少关键交互**

- 无拖拽上传（当前仅点击上传）
- 无图片放大/缩放查看（遥感图像通常很大，需要细节查看）
- 无检测框点击交互（点击框应高亮并显示详情）
- 无结果筛选（按类别、按置信度过滤检测框）

### 3.3 性能优化

| 问题 | 优化方案 |
|------|----------|
| 历史记录无缓存 | 引入 Pinia store 缓存 + Vue Query 或手写 dedup |
| 大列表未虚拟化 | 长列表 (历史记录、检测框) 使用 `vue-virtual-scroller` |
| 图片无懒加载 | 缩略图列表使用 `v-lazy` 或 Intersection Observer |
| 无请求防抖/节流 | 搜索输入虽有 debounce 300ms，但其他交互未做 |
| Element Plus 全量引入 | 虽配了 unplugin 按需导入，但图标库仍可能是全量 |

### 3.4 UI/UX 优化

| 方向 | 具体措施 |
|------|----------|
| **加载态** | 统一骨架屏 (Skeleton) 替代转圈 spinner |
| **空状态** | 各页面空状态引导文案和操作入口需统一风格 |
| **错误处理** | 统一错误提示组件，区分网络错误/业务错误/权限错误 |
| **响应式** | 当前仅桌面端适配，需补充移动端/平板布局 |
| **快捷键** | 检测页面：Ctrl+Enter 提交、Esc 关闭弹窗等 |
| **通知** | 检测完成/批量任务完成的后台通知（即使切换页面） |
| **暗色模式** | 已有基础支持，但部分硬编码颜色未走 CSS 变量 |

### 3.5 工程化

| 缺失项 | 建议 |
|--------|------|
| TypeScript | 从 JS 迁移到 TS，提升类型安全 |
| ESLint + Prettier | 代码规范统一 |
| 单元测试 | Vitest + Vue Test Utils |
| E2E 测试 | Playwright 或 Cypress |
| CI/CD | GitHub Actions 自动构建/部署 |
| 环境变量 | 区分 .env.development / .env.production |
| Git hooks | husky + lint-staged 提交前检查 |

---

## 四、总结优先级路线图

```
Phase 1 (基础补全):      真实认证 → 数据库接入 → MinIO 对象存储
Phase 2 (核心功能):      交互地图 → 手动标注 → 标注导出 → 置信度调节
Phase 3 (体验提升):      前端状态管理重构 → 清理遗留代码 → SliderCompare集成 → 拖拽上传
Phase 4 (平台完善):      模型评估面板 → 报告导出 → 项目管理 → 通知系统
Phase 5 (竞争力):       变化检测 → AI辅助标注 → 自定义模型 → 大图切片推理
```
